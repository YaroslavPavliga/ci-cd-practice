{
  "name": "ci-cd-practice",
  "version": "1.0.0",
  "private": true,
  "description": "Practice CI/CD pipeline with GitHub Actions",
  "scripts": {
    "test": "echo \"tests passed\"",
    "build": "mkdir -p dist && echo \"build artifact\" > dist/app.txt"
  } 
}
