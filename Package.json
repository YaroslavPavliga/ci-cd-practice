{
  "name": "ci-cd-practice",
  "version": "1.0.0",
  "private": true,
  "description": "Practice CI/CD pipeline with GitHub Actions",
  "scripts": {
    "test": "echo \"Running tests\" && exit 0",
    "build": "echo \"Building project...\" && mkdir -p dist && echo 'build ok' > dist/index.html"
  }
}
 
