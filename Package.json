{
  "name": "ci-cd-demo",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha",
    "build": "echo Build finished && mkdir -p dist && echo OK > dist/app.txt"
  },
"devDependencies": {
  "mocha": "^10.0.0",
  "mocha-junit-reporter": "^2.2.0"
}
  }
